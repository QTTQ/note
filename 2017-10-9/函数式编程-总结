01 我们在做什么?

结合律 add(add(x, y), z) == add(x, add(y, z));
交换律 add(x, y) == add(y, x);
同一律 add(x, 0) == x;
分配律 mul(x, add(y, z)) == add(mul(x, y), mul(x, z));
02 函数是一等公民

03 纯函数的好处

纯函数定义: 相同的输入，永远得到相同的输出。而且没有任何可观察的副作用 追求"纯"的理由

可缓存性
可移植性/自文档化
可测试性
合理性
并行代码
04 柯里化

概念: 传递给函数一部分参数来调用它，让它返回一个函数去处理剩下的参数